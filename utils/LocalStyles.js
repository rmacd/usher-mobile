import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    defaultSpacing: {
        marginBottom: 15,
        lineHeight: 20,
    },
    boldText: {
        fontWeight: 'bold',
    },
    muted: {
        fontFamily: 'Courier',
        color: '#aaa',
    },
});
